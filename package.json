{
  "name": "onegraph-changelog",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@tippy.js/react": "^2.2.2",
    "babel-plugin-relay": "^5.0.0",
    "date-fns": "^1.30.1",
    "flow-bin": "^0.105.1",
    "graphql": "^14.4.2",
    "onegraph-auth": "^2.0.0",
    "prettier": "^1.18.2",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-markdown": "^4.1.0",
    "react-relay": "^5.0.0",
    "react-scripts": "3.1.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "relay": "relay-compiler --verbose --src ./src --schema ./schema.json --persistFunction scripts/persistQuery.js",
    "flow": "flow",
    "fetch-schema": "node scripts/fetchSchema.js --path schema.graphql"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "relay-compiler": "^5.0.0"
  }
}
